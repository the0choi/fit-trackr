<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter&family=Open+Sans:wght@400;800&display=swap" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter&family=Nunito&family=Open+Sans:wght@400;800&display=swap" rel="stylesheet">  
  <link rel='stylesheet' href='/stylesheets/output.css' />
  <title>Trackr</title>
</head>
<body class="bg-gradient-to-b from-gray-800 to-gray-700 h-screen text-white">

    <nav class="bg-gray-800 mx-auto px-4">
        <div class="relative flex h-16 items-center justify-between">
            <div class="flex flex-1 items-stretch justify-start">
                <div class="flex">
                    <img class="h-8 w-auto px-4" src="https://tailwindui.com/img/logos/mark.svg?color=indigo&shade=500">
                </div>
                <div class="flex space-x-4">
                    <% if (user) { %>
                        <a href="/dashboard" class="nav-link">Dashboard</a>
                        <a href="/health/new" class="nav-link">Daily Log</a>
                        <a href="/auth/fitbit" class="nav-link">Connect to Fitbit</a>
                    <% } %>
                </div>
            </div>

            <div class="absolute inset-y-0 right-0 flex items-center pr-2">
                <% if (user) { %>
                    <a href="/" class="nav-link">Log Out</a>
                <% } else { %>
                    <a href="/auth/google" class="nav-link">Log In</a>
                <% } %>
                
            </div>
        </div>
    </nav>

  <main>
    <br>
  </main>